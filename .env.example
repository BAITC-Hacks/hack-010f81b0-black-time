EKT_LOGIN=apiuser
EKT_PASSWORD=""

# Search covers a representative sample, not the entire catalog (1 to 20 pages).
CATALOG_MAX_PAGES=3
# Set ONLY after the API owner confirms the currency. Empty means unknown.
CATALOG_CURRENCY=""
