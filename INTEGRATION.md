# Каталогты backend серверіне қосу

Frontend дайын: каталогты беттермен оқу, тауарды ID арқылы ашу, жүктелу/қате/қайта көру/бос тізім көріністері бар. Нақты қосылуды аяқтау үшін backend адресі және екі нақты JSON жауабы қажет. Қазіргі демо деректер backend жауабының дәл үлгісі емес.

## Келісілген екі маршрут

| Әрекет | HTTP сұранысы | JSON ішіндегі дерек |
| --- | --- | --- |
| Каталогтың бір беті | `GET /products?page=1` | `data.items` массиві |
| Тауардың толық мәліметі | `GET /products/515291` | `data` объектісі |

`fetch()` нәтижесі алдымен `response.json()` арқылы JSON-ға айналады. `data.items` және `data` жолдары осы талданған JSON-ға қатысты.

Толық мәлімет маршрутына тауардың `id` мәні жіберіледі. Frontend ID-ді жолға (`string`) келтіреді; сан немесе жол болып келген ID бірдей қолданылады. Артикул (`sku`) `id` орнына қолданылмайды.

## Баптау

`index.html` ішінен `CATALOG_CONFIG` табыңыз. Бастапқы режим — `mode: 'demo'`, сервер адресі — бос жол. Демо каталог екі беттен, әр бетте 20 синтетикалық тауардан тұрады. Екінші беттің бірінші ID-і `515291` болғанымен, оның атауы, бағасы, қалдығы және сипаттамалары нақты API-ден алынбаған.

Backend нақты деректерді бергеннен кейін конфигурацияны төмендегі үлгі бойынша толтырыңыз:

```js
const CATALOG_CONFIG = {
  mode: 'api',
  baseUrl: 'https://YOUR-BACKEND.example',
  timeoutMs: 10000,
  fields: {
    name: 'name',
    sku: 'sku',
    brand: 'brand',
    price: 'price',
    stock: 'stock',
    unit: 'unit',
    description: 'description',
    specs: 'specs',
    image: 'image',
    certificates: 'certificates'
  },
  pagination: {
    hasNext: 'has_next',
    totalPages: 'total_pages'
  }
};
```

**Бұл — баптау үлгісі.** `https://YOUR-BACKEND.example` орнына команда берген нақты Base URL жазылады. `fields` және `pagination` ішіндегі атаулар да әзірге болжамды; олар backend-тің расталған схемасы емес. Нақты JSON-мен салыстырып, қажетті өрістерді сәйкестендіріңіз. Егер дерек құрылымы күрделі болса, мысалы қалдық қоймалар массивінде немесе баға жеке объектіде берілсе, каталог адаптерін сол құрылымға сай өзгерту керек.

Жай ішкі объектілерге нүктемен жол көрсетуге болады: мысалы, `price: 'pricing.amount'`. `fields` жолдары әр тауар объектісіне, `pagination` жолдары жауаптың `data` объектісіне қатысты. `id` тікелей тауардың `id` өрісінен оқылады. Баға қазіргі интерфейсте теңгемен көрсетіледі; нақты жауап соманы теңгемен бере ме, әлде басқа бірлікпен бере ме — интеграция алдында тексеріңіз.

`specs` үшін `{ "Сипаттама": "Мән" }` объектісі немесе `[{ "label": "Сипаттама", "value": "Мән" }]` массиві қолданылады. `certificates` үшін URL жолдары не `{ "name": "Атауы", "url": "https://…" }` элементтері қабылданады. Бұл пішімдер нақты backend үшін расталмаған; басқа құрылым келсе, `normalizeProduct()` ішіндегі түрлендіруді сәйкестендіру керек.

Frontend үшін сервер адресі `/products` маршруттарына дейінгі ортақ бөлік болуы керек. Мысалы, backend маршруттары `/api/products` арқылы жарияланса, Base URL сол `/api` префиксін қамтиды. Бұл префиксті backend жасаушысымен нақтылаңыз.

1. `mode` мәнін `'api'` етіп, нақты Base URL және өріс сәйкестігін орнатыңыз.
2. VS Code Live Server арқылы `index.html` файлын HTTP адресімен ашыңыз. API режимін файлға екі рет басып ашылған `file://` беттен тексермеңіз.
3. Backend-ке браузердегі frontend origin-ін жіберіңіз: протокол, хост және порт. Мысалы, Live Server адресі `http://127.0.0.1:5500` болса, CORS сол origin-ге рұқсат беруі керек; `localhost` басқа origin болып есептеледі.
4. Бірінші және екінші бетті, нақты ID бойынша мәліметті, қате және бос нәтиже көріністерін тексеріңіз. Нақты сервердің қолжетімділігі әлі тексерілген жоқ.

HTTPS беттен ашылатын frontend үшін backend те HTTPS арқылы қолжетімді болсын. Әйтпесе браузер HTTP сұранысын бұғаттауы мүмкін.

## Деректерді көрсету ережелері

- Баға немесе қалдық берілмесе, интерфейс оларды белгісіз деп көрсетеді. Деректің жоқтығы нөлге немесе «қоймада жоқ» мәртебесіне айналмайды.
- Баға валютасы, бөлшек сан форматы және қалдықтың өлшем бірлігі backend-пен келісілуі керек. Бірнеше қойма болғанда көрсетілетін қалдықтың мағынасын да нақтылау қажет.
- Келесі беттің бар-жоғы туралы метадеректі келісіңіз. `has_next` және `total_pages` — тек бастапқы атау мысалдары. Бір бетте 20 тауар болуы келесі беттің міндетті түрде бар екенін дәлелдемейді.
- Сыртқы суреттер үшін тек `http:` және `https:` URL қолданылады. Сурет болмағанда placeholder көрінеді.
- `api` режиміндегі қате демо дерекпен жасырылмайды: пайдаланушы қате хабарламасын және қайта көру әрекетін алады.
- Каталогтың нақты тауарлары демо чат пен себетке берілмейді. Нақты себет интеграциясы жеке endpoint-термен және растау келісімімен жасалады.

## Backend-тен қажет мәліметтер

- [ ] Толық Base URL, ортақ маршрут префиксі және сервердің қолжетімді ортасы.
- [ ] `GET /products?page=1` сұранысының нақты JSON жауабы.
- [ ] `GET /products/515291` сұранысының нақты JSON жауабы немесе қолжетімді басқа нақты ID.
- [ ] Тауар өрістері: атау, артикул, бренд, баға, валюта, қалдық/қоймалар, өлшем бірлігі, сипаттама, техникалық сипаттамалар, сурет және сертификаттар.
- [ ] Пагинация: ағымдағы бет, жалпы бет саны немесе келесі беттің бар-жоғы; соңғы беттен кейінгі жауап.
- [ ] `null`, бос мән, тауар табылмауы және қате жауаптарының форматы мен HTTP мәртебелері.
- [ ] Frontend origin-іне CORS рұқсаты және авторизацияның қажет-қажет еместігі; қажет болса, браузерге арналған келісілген тәсілі.

ekt.kz BasicAuth деректері frontend файлына, браузер сұраныстарына немесе Git репозиторийіне енгізілмейді. Frontend команданың backend серверімен жұмыс істейді; ekt.kz-ке қатынау сол backend жағында орындалады. Клиенттің төлем деректері сұралмайды.

## Backend жасаушысына жіберуге дайын хабарлама

> Сәлем! Каталогқа арналған frontend клиенті дайын. Нақты серверге қосу үшін backend-тің толық Base URL адресін, `GET /products?page=1` және `GET /products/515291` сұраныстарының нақты JSON жауаптарын жіберіңізші. Тізімді `data.items`, толық мәліметті `data` ішінен оқимын; мәлімет маршрутына артикул емес, `id` жіберемін. Тауар атауы, баға/валюта, қалдық/қоймалар, өлшем бірлігі, сурет, сипаттамалар және сертификаттар қай өрістерде екенін де нақтылайық. Соңғы бетті қалай анықтаймыз: келесі бет белгісі немесе жалпы бет саны бар ма? Frontend адресіне CORS рұқсатын және авторизация қажет пе екенін хабарлаңызшы. Әзірге чат пен себет демо күйінде, олардың API келісімін келесі кезеңде біріктіреміз.

## Кейін біріктірілетін бөліктер

Нақты іздеу, ЖИ жауаптары, сервердегі себет, тапсырыс және авторизацияланған тарих бұл кезеңге кірмейді. Оларға endpoint-тер мен жауап схемалары берілгенде бөлек қосылады. Себет үшін клиенттің нақты растауы, сервердегі өзекті қалдық пен бағаны қайта тексеру және қайталанған сұраныстан қорғану келісімі қажет болады.
