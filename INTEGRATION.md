# Біріктірілген интерфейс пен сервер келісімі

`Frontend`, `backend`, `Ai-selection` бөліктері `integration` тармағында бір қолданбаға қосылған. Орнату және бастаушыға арналған толық нұсқаулық — [README.md](README.md).

## Бір сервер

Жоба түбірінен:

```powershell
.\.venv\Scripts\python.exe -m uvicorn main:app --app-dir backend --reload --host 127.0.0.1 --port 8000
```

Браузер: **http://127.0.0.1:8000/**. FastAPI `index.html`, `/frontend/*` және API маршруттарын береді. Live Server, `file://` немесе бөлек frontend порты қажет емес. Браузер `/api/*` және `/products*` салыстырмалы мекенжайларын қолданады; құпиялар браузерге берілмейді.

## Негізгі келісім

1. `GET /api/session` → `HttpOnly; SameSite=Strict` cookie, `cart_mode: "prototype"`, `ai_mode: "rules"`, файл мүмкіндіктері. HTTPS кезінде cookie `Secure` болады.
2. `GET /products?page=N` → `{source:"ekt.kz", data:{page,per_page,count,items}}`; `GET /products/{id}` → `{source:"ekt.kz", data:product}`. Бұл екі маршрут бастапқы JSON-ды өзгертпей сақтайды.
3. `GET /api/search?q=...` → `{items:[normalizedProduct], coverage:{pages_loaded,max_pages,complete}}`.
4. `POST /api/chat` → `{message, request_id, confirmation_id?}`. `request_id` — жаңа әрекетке жаңа UUID; желілік қайталауда сол ID және дәл сол дене.
5. `POST /api/cart/proposal` → `{product_id, quantity, request_id}`. ID мен сан — оң бүтін сандар. Ұсыныс себетті өзгертпейді.
6. `POST /api/cart/confirm` → `{confirmation_id, request_id}`. Сервер токенді, сессияны, жаңа бағаны, ең аз санды және себеттегі санмен бірге қалдықты тексереді.
7. `GET /api/cart` → себет объектісі. `POST /api/cart/cancel` (`{}`) ұсынысты жояды. `DELETE /api/cart/items/{id}` осы сессияның себетінен тауарды алып тастайды.
8. `POST /api/attachments` → multipart `file`; жауап `{filename,text,notes}`. Бұл тек өңделетін жоба мәтіні; жаңа файл ескі растауды жояды және себетті өзгертпейді.

Чат, ұсыныс және растаудың ортақ жауабы:

```text
{
  reply: { message, locale, cards: [normalizedProduct] },
  confirmation: null | { id, product_id, quantity, product },
  cart: {
    mode: "prototype",
    items: [{ product, quantity, line_total }],
    total, currency, cart_url: "/cart"
  }
}
```

`confirmation.id` UI-дегі растау батырмасына байланады. «Иә» чат хабарламасы да сол токенмен жіберіледі. Тауар/сан өзгерсе, ескі ұсыныс қолданылмайды. Растаудағы баға/қор өзгерісі жаңа ұсынысты талап етеді. Бірдей request-id-мен қайталау қосымша тауар қоспайды; басқа сұраныс денесіне сол ID-ді қайта қолдану — `409`.

## Дерек адаптері

| Нақты API | Ортақ модель |
| --- | --- |
| `id` | `id`; артикулмен алмастырылмайды |
| `article` | `sku` |
| `quantity` | `stock` |
| `properties` | `attributes` |
| `stores` | `warehouse_stocks` |
| `properties.KRATNOST_MIN` | `minimum_order` |
| `price`, `image`, `url`, `description` | Сол мағынадағы өрістер |
| `currency` не расталған `CATALOG_CURRENCY` | `currency`; екеуі де жоқ болса `null` |

Белгісіз баға/қалдық нөлге айналмайды. Сертификат тек нақты `certificate_url` дерегінен беріледі. Атаудағы ток пен `NOMINALNYY_TOK` қайшылығы `warnings` ішінде көрсетіледі; мұндай тауарға автоматты аналог сәйкестігі жасалмайды.

Іздеу әдепкіде алғашқы **3 бетке** шектелген, бет кэші — **120 секунд**. `CATALOG_MAX_PAGES` шегі 1–20. `count` — бүкіл каталогтың саны деп қабылданбайды. Нақты ID-ге `ID 515291`, нақты артикулға `артикул 200300285_` пішімін қолданыңыз. Іріктемеде табылмаған тауар бүкіл дүкенде жоқ деп жарияланбайды.

Python backend `src/bridge.js` арқылы команданың `src/dialogue.js`/`src/nlu.js` ережелерін орындайды. LLM және ЖИ провайдері шақырылмайды. Баға, қор, сертификат немесе техникалық сәйкестік ойдан жасалмайды. Аналог үшін категория, берілген негізгі сипаттамалар және белгілі оң қалдық қажет; кей сұрауларға аналог табылмауы мүмкін.

## Сессия, файлдар және шектеулер

- Сессия, диалог және себет сервер жадында. Белсенді емес сессия 1 сағаттан кейін жойылады; сервер қайта іске қосылса, бәрі жоғалады. Көп worker/production үшін ортақ сақтау қажет.
- Себет **тек прототип**, `/cart` осы прототипті ашады. Нақты ekt.kz cart/checkout интеграциясы жоқ: сыртқы write API мен клиент сессиясының келісімі берілмеген. Ұйымдастырушылардың мұны қабылдайтыны расталмаған.
- Төлем/жеткізу дерегі [backend/purchase_terms.json](backend/purchase_terms.json) ішінде. [Ресми беттің](https://ekt.kz/include/ses.php) іздеу индексіндегі үзінді қолданылған; тікелей сұраныс таймаутпен аяқталған. Дүкеннен қайта растау қажет; нақты тариф, мерзім және жалпы ең аз тапсырыс сомасы уәде етілмейді.
- XLSX/XLS, DOCX, мәтін қабаты бар PDF және Windows OCR арқылы JPEG оқылады. 10 МБ файл/10 000 таңба шегі бар. PDF алғашқы 10 бетпен шектелген; скан PDF OCR, ескі `.doc`, PNG және фотодан тауар моделін тану жоқ. Алдын ала қарау мен қолмен тексеру қажет.
- API кілттері/BasicAuth тек `.env` не сервер ортасында. Клиенттің төлем деректері сұралмайды; карта нөміріне ұқсас чат енгізуі жауапта қайталанбайды және диалог/replay тарихында сақталмайды.
- `401` — сессия жоқ; `403` — рұқсатсыз origin; `409` — растау/себет шарты өзгерген; `413/415/422` — файл/параметр мәселесі; `502/503/504` — upstream, баптау немесе уақыт шегі. Қате мәтіні `detail` ішінде.

## Командалық тексеру

```powershell
.\.venv\Scripts\python.exe -m pytest -q
npm.cmd test
```

Python сынақтары нақты Node диалогын шақырады, бірақ каталогты жасанды дерекпен ауыстырып, сыртқы Requests желісін бұғаттайды. JPEG сынағы Windows-та нақты OCR қолданады. Қолмен толық жол: **тауар сұрау → карточка → сан ұсынысы → нақты растау → өзекті қор тексеруі → прототип себеті → `/cart`**. Оны компьютер мен тар экранда тексеріңіз. Каталогтың бұрынғы нақты жауаптары бар болғанымен, тесттердің өтуі нақты ekt.kz себеті қосылды немесе барлық талап толық орындалды дегенді білдірмейді.
